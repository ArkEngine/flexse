这是一个连接池，与flexse没有任何关系
只是用到了flexse的一个连接超时的函数
特性
(1) 支持长连接和短连接
(2) 支持故障服务屏蔽(减少连接概率)，支持故障机器恢复后自动加入
(3) 支持负载均衡算法(随机和CARP算法)，每次可以按照一个负载均衡的key来选取服务，有利于提高cache的命中率
(4) 支持连接超时，内部重试以及套接口合法性检查
(5) 接口简单，FetchSocket()和FreeSocket()
